import React from 'react';

export default function comments() {
  return (
    <div>I do not think this page will actually get used,
      We do need [id?] for a comment page related to the book
      I am not sure what the routing would be for that -ds
    </div>
  );
}
